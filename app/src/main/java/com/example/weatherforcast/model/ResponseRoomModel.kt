/*
package com.example.weatherforcast.model

import androidx.room.Entity

@Entity(tableName = "response")
data class ResponseRoomModel (
    val daily: List<Daily>,
    val hourly: List<Hourly>,
    val current: Current
        )*/
